{
  "name": "aab-student-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/attendance.js --bundle --minify --format=esm --outfile=assets/attendance.js",
    "test": "node --test tests/*.test.js",
    "test:db": "bash scripts/test-database.sh"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.57.4",
    "qrcode": "1.5.4"
  },
  "devDependencies": {
    "esbuild": "0.25.10",
    "prettier": "3.6.2"
  }
}
